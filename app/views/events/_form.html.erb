<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :event_content %>
  </div>

  <div class="field">
    <%= form.label :targetable_id %>
    <%= form.text_field :targetable_id, id: :event_targetable_id %>
  </div>

  <div class="field">
    <%= form.label :resultable_id %>
    <%= form.text_field :resultable_id, id: :event_resultable_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :event_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
